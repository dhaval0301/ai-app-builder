# VITE_API_URL is only needed when the backend is on a different domain.
# Leave empty for local dev (Vite proxy handles /api → localhost:8000).
# On Vercel, leave empty too — vercel.json rewrites /api to your Railway URL.
# Only set this if you want to bypass the Vercel rewrite.
VITE_API_URL=
